import styled from "styled-components";

const DocumentListWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 0 4px;
`;

const DocumentCount = styled.div`
  padding: 0 4px;
  font-weight: 700;
  font-size: 15px;
`;

export { DocumentCount, DocumentListWrapper };
